<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cc.word.dao.user.UserDao">

    <insert id="save" parameterType="com.cc.word.model.user.User">
        INSERT INTO user (username, password, head_img, create_time)
        values (#{username,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR}, #{headImg,jdbcType=VARCHAR},
        #{createTime,jdbcType=TIMESTAMP})
    </insert>
    
    <select id="selectUser" parameterType="String" resultType="com.cc.word.model.user.User">
        SELECT id,username,password,head_img,create_time
        FROM user
        WHERE 1=1
        <if test="username != null and username != '' ">
            AND username = #{username}
        </if>
        <if test="password != null and password != '' ">
            AND password = #{password}
        </if>
    </select>

    <select id="selectUserInfoById" parameterType="Integer" resultType="com.cc.word.model.user.User">
        SELECT id,username,password,head_img,create_time
        FROM user
        WHERE id = #{userId}
    </select>

</mapper>