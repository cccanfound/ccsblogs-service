<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cc.word.dao.dashboard.DashboardDao">

    <select id="selectDashboardList" parameterType="map" resultType="map">
        SELECT essay.id id,user.username,title,DATE_FORMAT(essay.create_time,'%m-%d-%Y') create_time,essay.picture
        FROM essay
        LEFT JOIN user on user.id = essay.author_id
        where type = '1'
        ORDER BY id desc
    </select>
</mapper>